[package]
name = "talos-trace-nats"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
async-nats = "0.47"
opentelemetry = "0.32"
tracing = "0.1"
tracing-opentelemetry = "0.33"

[dev-dependencies]
# Provides the concrete W3C `TraceContextPropagator` used to exercise the
# inject/extract round-trip in tests (in production `talos_trace::init_tracing`
# installs it as the global propagator).
opentelemetry_sdk = "0.32"

[lints]
workspace = true
