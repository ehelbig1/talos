{
  "name": "alert-normalize-email",
  "version": "v1.0.0",
  "display_name": "Alert Normalize (Email)",
  "description": "Normalize alert-bearing emails (Snyk vulnerability alerts, AWS Health events, ServiceNow case assignments, generic monitoring mail) from a Gmail: List Messages batch into ops-alerts. Emits the __ops_alert__ envelope so the engine persists them into the ops_alerts triage store with dedup + severity heuristics; non-alert mail is ignored. Pure compute (minimal-node) — pair upstream with Gmail: List Messages and triage via list_ops_alerts / correct_ops_alert_severity.",
  "category": "Operations",
  "capability_world": "minimal-node",
  "allowed_hosts": [],
  "requires_secrets": [],
  "config_schema": {
    "type": "object",
    "properties": {
      "SOURCE_PREFIX": {
        "type": "string",
        "default": "",
        "description": "Optional prefix for the emitted source labels (e.g. 'work-' → 'work-snyk-email')"
      },
      "INCLUDE_GENERIC": {
        "type": "boolean",
        "default": false,
        "description": "When true, mail matching generic alert keywords (alert/incident/outage/failure in the subject) that no specific parser claims is emitted as source 'generic-email' with severity unclassified"
      },
      "EMIT_OPS_ALERTS": {
        "type": "boolean",
        "default": true,
        "description": "When true (default), the parsed alerts ride out under the __ops_alert__ engine-hook key so THIS node ingests them into the ops_alerts store (standalone deployment). Set false to emit the same alerts under a plain 'alerts' key instead — hand it to a downstream 'Hybrid Classify (Alerts)' node, which becomes the sole __ops_alert__ emitter and decides severity via the ops-severity classifier + LLM fallback at first ingest."
      }
    }
  }
}
