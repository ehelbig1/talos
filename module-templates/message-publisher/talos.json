{
  "name": "message-publisher",
  "version": "v1.0.0",
  "display_name": "Message Publisher",
  "description": "Publish events or JSON messages directly to the Talos NATS message broker or external systems.",
  "category": "Network",
  "capability_world": "messaging-node",
  "config_schema": {
    "type": "object",
    "required": ["topic", "message"],
    "properties": {
      "topic": {
        "type": "string",
        "description": "The message topic/subject to publish to"
      },
      "message": {
        "type": "string",
        "description": "The JSON payload or text to publish"
      },
      "add_metadata": {
        "type": "boolean",
        "default": true,
        "description": "Automatically append workflow execution context"
      }
    }
  }
}
