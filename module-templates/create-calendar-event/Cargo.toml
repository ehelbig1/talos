[package]
name = "create-calendar-event"
version = "0.1.0"
edition = "2021"

[dependencies]
urlencoding = "2.1.3"
wit-bindgen-rt = { version = "0.44.0", features = ["bitflags"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
talos_sdk_macros = { path = "../../talos_sdk_macros" }

[lib]
crate-type = ["cdylib"]
path = "template.rs"

[package.metadata.component]
package = "talos:create-calendar-event"

[package.metadata.component.target]
path = "../../wit/talos.wit"
world = "network-node"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[workspace]
