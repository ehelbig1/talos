[package]
name = "talos-ops-alert-store"
description = "The one ops_alerts write (the dedup upsert) and the actor-tenancy read it needs, below the actor and workflow repositories so a budget refusal can raise an alert"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde_json.workspace = true
sqlx = { version = "0.8", default-features = false, features = ["postgres", "runtime-tokio-rustls", "uuid", "json", "chrono"] }
thiserror = "2.0"
uuid.workspace = true

[lints]
workspace = true
