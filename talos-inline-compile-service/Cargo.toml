[package]
name = "talos-inline-compile-service"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2 = "0.10"
sqlx = { version = "0.8", default-features = false, features = ["postgres", "runtime-tokio-rustls", "uuid"] }
thiserror.workspace = true
tracing.workspace = true
uuid = { version = "1", features = ["v4", "serde"] }

# Talos crates — repositories
talos-actor-repository = { path = "../talos-actor-repository" }
talos-module-repository = { path = "../talos-module-repository" }
talos-workflow-repository = { path = "../talos-workflow-repository" }

# Talos crates — services
talos-capability-world = { path = "../talos-capability-world" }
talos-compilation = { path = "../talos-compilation" }
talos-workflow-creation-helpers = { path = "../talos-workflow-creation-helpers" }

[features]
# `CompiledInline::prebuilt_for_tests` — lets a DB test drive
# `persist_compiled` (refusal at persist, the persisted grants) without a WASM
# toolchain. Enabled only by `controller`'s dev-dependencies.
test-support = []

[lints]
workspace = true
