[package]
name = "talos-shutdown"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
anyhow.workspace = true
tokio = { version = "1", features = ["sync", "signal", "macros", "rt", "time"] }
# `CancellationToken` for the per-run stop signal `inflight::InFlightRuns::cancel`
# fires (an operator cancel reaching the engine driving the run).
tokio-util = { version = "0.7", default-features = false }
uuid.workspace = true
tracing.workspace = true

[lints]
workspace = true
